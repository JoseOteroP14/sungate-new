# Copy to `.env` for local Compose. Never commit `.env`.
# Production values live in GitHub Actions secrets/variables — see docs/github-actions.md.

POSTGRES_USER=sungate
POSTGRES_PASSWORD=change-me
POSTGRES_DB=sungate

# Public site URL (scheme + host). Used for docs/links, not for TLS routing.
DOMAIN=https://sungate.salesconnect.dev
